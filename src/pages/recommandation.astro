---
import BaseLayout from "@/layouts/BaseLayout.astro";
import StrategicMatcher from "@/components/matcher/StrategicMatcher.tsx";
import { getAllVehicles } from "@/lib/vehicles";

const vehicles = getAllVehicles();
---

<BaseLayout
  title="Recommandation & Matcher Stratégique | Evly"
  description="Identifiez le véhicule électrique idéal adapté à votre budget, vos trajets réels, vos modes de recharge et votre éligibilité aux aides d'État."
>
  <div class="container-edit py-6 md:py-10">
    <div class="flex items-center gap-4 mb-6 fade-rise">
      <span class="font-mono text-xs text-[var(--color-text-faint)]">05</span>
      <span class="label-eyebrow">Recommandation</span>
    </div>
    
    <div class="fade-rise fade-rise-delay-1">
      <StrategicMatcher client:load vehicles={vehicles} />
    </div>
  </div>
</BaseLayout>

